ℕ = τ {
    0    : ℕ
    succ : ℕ → ℕ
}

plus = λ x : ℕ. λ { 
    (succ y) : ℕ. succ (plus x y)
    0        : ℕ. x
}

ℕ𝓁 = τ { 
    nil : ℕ𝓁
    cons: ℕ𝓁 → ℕ → ℕ𝓁
}

𝔹 = τ { 
    true  : 𝔹
    false : 𝔹
}
